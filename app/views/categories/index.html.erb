<div class="todoListContainer">
<h2>Categories</h2>


<ul>
  <% @todolists_count.each do |k, u| %>
  
    <h4>&#x27a4; <%= link_to "#{k} (#{u})", user_category_path(@user,k) %> </h4>

  
  <% end %>
</ul>
</div>

