<%= form_for([@user, @todolist]) do |f| %>
  <div class="addContainer">


    <%= f.text_field :title,  :class => 'userInfo'  %>


      <%= f.text_field :category, :class => 'userInfo'  %>


        <%= f.text_field :description,  :class => 'userInfo'  %>

          <div class="buttons">
            <button type="submit"><%= f.submit 'Update Item', :class => 'Submit' %></button>
          </div>
          <div class="buttons">
            <%= link_to '↼ Back to List', user_todolists_path %>
          </div>
          <% end %>
  </div>
